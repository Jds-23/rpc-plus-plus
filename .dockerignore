# Keep the build context small and keep secrets out of it.
target/

# Provider URLs carry API keys. Never let this reach a layer.
settings.yaml

.git/
.github/
.claude/
docs/
docker/
scripts/
tests/
*.md
Dockerfile
.dockerignore
